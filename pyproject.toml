[project]
name = "lyfter"
version = "0.1.0"
description = "Python programs for the Lyfter workspace."
requires-python = ">=3.12"
dependencies = []

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
